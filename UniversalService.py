from DB_requests import DB_requests
from DB_connection import DB_connection

class UniversalService:
    def __init__(self, entry):
        super().__init__(entryID)

    def add_entry(self):
        if(DB_connection.connection == True):
            print("Universal service added to repo")

    def modify_entry(self):
        if(DB_connection.connection == True):
            print("Universal service modified in repo")

    def delete_entry(self):
        if(DB_connection.connection == True):
            print("Universal service removed from repo")
    def search_entry(self):
        if(DB_connection.connection == True):
            print("Serching for universal service in repo")